autocomplete-project/
│
├── src/
│ ├── autoCompleteTrie.js ← הקובץ של הקלאס הראשי
│ └── app.js ← קונסולת המשתמש (UI - ממשק טקסטואלי)
│
├── tests/
│ └── autoCompleteTrie.test.js ← בדיקות יחידה (unit tests)
│
├── package.json ← ניהול חבילות (npm, jest וכו')
└── README.md ← תיעוד הפרויקט
